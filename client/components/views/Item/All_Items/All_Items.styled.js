import styled from "styled-components";

export const StyledAll_Items = styled.section`
  width: 100%;
  padding-top: 50px;
  ${
    "" /* div:hover {
    position: relative;
    a {
      @keyframes item-hover {
        0% {
          transform: scale(1);
        }

        100% {
          transform: scale(1.1);
        }
      }
      z-index: 100;
      animation: item-hover 0.5s ease-in-out 0s;
      animation-fill-mode: forwards;
    }
  } */
  }
`;
